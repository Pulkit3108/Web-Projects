# TextUtils

A React utility for transforming and inspecting text.

## Run Locally

```bash
npm ci
npm start
```

The app runs at `http://localhost:3000`.

## Commands

- `npm start` — development server
- `npm run build` — production build
- `npm test` — test runner

See the [root project index](../README.md) for a screenshot.
