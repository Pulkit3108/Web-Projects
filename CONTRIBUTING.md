# Contributing

Keep changes scoped to one project directory.

- Do not commit `node_modules`, build output, API keys, database URLs, or JWT secrets.
- Keep each project README aligned with its own `package.json` scripts.
- Run the narrowest relevant build or test before opening a pull request.
